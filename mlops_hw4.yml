name: mlops_hw2
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.11  # Specify your Python version
  - pip
  - pip:
    - -e .       # Instructs pip to install the current directory/pyproject.toml
